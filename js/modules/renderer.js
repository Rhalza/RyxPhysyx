const Renderer=(function(){const render=function(ctx,world,width,height){console.log(`[Renderer]RenderCycleStart;Rendering ${world.particles.length} particles.`);ctx.clearRect(0,0,width,height);ctx.fillStyle='rgba(0,0,0,0.85)';ctx.fillRect(0,0,width,height);for(const particle of world.particles){ctx.beginPath();ctx.arc(particle.position.x,particle.position.y,particle.radius,0,Math.PI*2);ctx.fillStyle=particle.color;ctx.shadowColor=particle.color;ctx.shadowBlur=15;ctx.fill();ctx.shadowBlur=0;}console.log('[Renderer]RenderCycleEnd.');};return{render:render};})();