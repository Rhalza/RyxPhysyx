window.onload=function(){const canvas=document.getElementById('ryx-canvas');const ctx=canvas.getContext('2d');let width,height;const world={particles:[],settings:{gravity:new Vector2(0,200),bounds:{}}};let lastTime=0;function init(){resize();for(let i=0;i<75;i++){const x=Math.random()*width;const y=Math.random()*height*0.5;const r=Math.random()*10+3;const m=r*r*0.1;world.particles.push(new Particle(x,y,m,r));}window.addEventListener('resize',resize);requestAnimationFrame(gameLoop);}function resize(){width=window.innerWidth;height=window.innerHeight;canvas.width=width;canvas.height=height;world.settings.bounds.width=width;world.settings.bounds.height=height;}function gameLoop(currentTime){const dt=(currentTime-lastTime)/1000;lastTime=currentTime;Physics.update(world,dt);Renderer.render(ctx,world,width,height);requestAnimationFrame(gameLoop);}init();};